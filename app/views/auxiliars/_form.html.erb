<%= form_for([@subaccount, @auxiliar]) do |f| %>
  <% if @auxiliar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auxiliar.errors.count, "error") %> prohibited this auxiliar from being saved:</h2>

      <ul>
      <% @auxiliar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
