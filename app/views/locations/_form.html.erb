<%= form_for([@person,@location]) do |f| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :city_id %>
    <%= autocomplete_field_tag 'city', '', autocomplete_city_name_person_locations_path(@person),class: 'form-control' %>

  </div>
    <div class="form-group col-sm-6" >
      <%= label_tag 'Ciudad', 'Ciudad', for: 'id_city' %>
      <%= autocomplete_field_tag 'city', '', people_autocomplete_city_name_path,class: 'form-control' , update_elements: {id: '#id_city'}%>
      <%= hidden_field_tag 'id_city' %>
    </div>

  <div class="form-group">
    <%= f.label :address %>
    <%= f.text_field :address, class: 'form-control' %>
  </div>

  <div class="actions form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
